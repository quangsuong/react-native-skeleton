// import inAppMessaging from '@react-native-firebase/in-app-messaging';

/**
 * enable or disable show in-app messaging
 */
export const setMessagesDisplaySuppressed = async (isDisplaySuppressed?: boolean) => {
  // await inAppMessaging().setMessagesDisplaySuppressed(isDisplaySuppressed || true);
};
